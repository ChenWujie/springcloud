server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.170.130:1111 #配置Nacos地址，为虚拟机nginx代理地址

management:
  endpoints:
    web:
      exposure:
        include: '*'